# Use the same-origin paths for a deployed frontend served by the project nginx proxy.
# For separate frontend/backend hosting, replace these with the public backend URLs.
VITE_API_URL=/api
VITE_UPLOADS_URL=/uploads
